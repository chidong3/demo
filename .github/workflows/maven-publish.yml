name: SelfTest

on:
  push:
    branches: [ "main" ]

jobs:
  selfcheck:
    runs-on: self-hosted
    steps:
    - name: hi 1
      run: |
        cd D:\dev\workspace\demo
        git pull
    - name: Build with Maven
      run: mvn -B package --file pom.xml
  build:
    runs-on: ubuntu-latest
    steps:
    - name: hi 2
      run: echo hi 2
